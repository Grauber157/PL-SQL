CREATE OR REPLACE TRIGGER trg_verificar_idade
BEFORE INSERT ON AMIGO
FOR EACH ROW
BEGIN

    IF :NEW.IDADE < 18 THEN
        RAISE_APPLICATION_ERROR(-20001, 'IDADE MINIMA INVALIDA');
    END IF;

END;